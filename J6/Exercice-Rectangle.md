# Exercice - Rectangle

**Objectif :**
L'objectif de cet exercice est de créer une classe **`Rectangle`** pour représenter un rectangle par sa largeur et sa hauteur, puis de surcharger les opérateurs **`==`** et **`!=`** pour comparer des rectangles en fonction de leur surface.

**Instructions :**

1. Créez une classe **`Rectangle`** avec les membres privés **`largeur`** et **`hauteur`** pour représenter les dimensions du rectangle.
2. Surchargez les opérateurs **`==`** et **`!=`** pour comparer des rectangles en fonction de leur surface (surface = largeur * hauteur).
3. Dans la fonction **`main()`**, créez deux objets **`Rectangle`**, comparez-les à l'aide des opérateurs **`==`** et **`!=`**, et affichez le résultat.